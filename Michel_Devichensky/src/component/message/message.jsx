import React from "react";

const Message = ({ messages }) => {
    const 
    return (
        <div>
        <strong>{message.name}:</strong>   
        {message.content}
        </div>
    )
} 
    
export default Message;